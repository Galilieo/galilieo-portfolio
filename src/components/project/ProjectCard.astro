---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import TagList from '../common/TagList.astro';

interface Props {
  project: CollectionEntry<'projects'>;
}

const { project } = Astro.props;
const monograms: Record<string, string> = {
  'heart-island': 'HI',
  'ai-chat-app': 'AI',
  'galilieo-portfolio': 'GA',
};
const monogram = monograms[project.id] ?? project.data.title.slice(0, 2).toUpperCase();
---

<article class="project-matrix-card reveal">
  <a class="project-matrix-card__link" href={`/projects/${project.id}/`}>
    <div class="project-matrix-card__visual" aria-hidden="true">
      {
        project.data.cover ? (
          <Image
            class:list={['project-matrix-card__cover', `project-matrix-card__cover--${project.id}`]}
            src={project.data.cover}
            alt=""
            widths={[320, 480, 720]}
            sizes="(max-width: 760px) calc(100vw - 32px), (max-width: 1020px) 46vw, 360px"
            loading="lazy"
          />
        ) : (
          <span class="project-matrix-card__monogram">{monogram}</span>
        )
      }
    </div>
    <div class="project-matrix-card__body">
      <div class="project-matrix-card__topline">
        <span>{String(project.data.order).padStart(2, '0')}</span>
        <span>{project.data.typeLabel}</span>
      </div>
      <h2>{project.data.title}</h2>
      <p class="project-matrix-card__description">{project.data.description}</p>
      <TagList items={project.data.stack.slice(0, 4)} label={`${project.data.title}技术栈`} />
      <footer class="project-matrix-card__footer">
        <span>{project.data.role}</span>
        <span>{project.data.status}</span>
        <span class="project-matrix-card__arrow" aria-hidden="true">→</span>
      </footer>
    </div>
  </a>
</article>
